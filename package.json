{
  "name": "zingchat",
  "version": "1.0.0",
  "description": "ZingChat — Real-time messaging app",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && node index.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  }
}
